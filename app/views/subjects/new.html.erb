<section class="flex flex-col justify-center content-center">
<h1 class =" text-center font-hairline mt-5 text-2xl"> <%= t('subject.new.new_subject') %></h1>
	<div class="w-2/3 text-lg mx-20 px-10 pt-10 ">
		<div class=" ">
			<%= render 'form', subject: @subject %>
		</div>
		<%= link_to t('general_buttons.back') , static_welcome_path %>
	</div>
</section>
